<!DOCTYPE html>
<html lang="en" dir="ltr">

<head>
    <meta charset="utf-8">
    <title>CampSite™</title>
    <link rel="stylesheet" href="/stylesheets/app.css">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
</head>

<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light mb-3">
        <div class="container">
            <a class="navbar-brand" href="/">🏕 CampSite™</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#toggler_items" aria-controls="toggler_items" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="toggler_items">
                <ul class="navbar-nav ml-auto mt-2 mt-lg-0">
                    <% if(!current_user) { %>
                        <li class="nav-item">
                            <a class="nav-link" href="/login">Login<span class="sr-only">(current)</span></a>
                        <li class="nav-item">
                            <a class="nav-link" href="/register">Sign Up</a>
                        </li>
                    <% } else {  %>
                        <span class="navbar-text mx-4">
                            Signed in as <%= current_user.username %>
                        </span>
                        <li class="nav-item">
                            <a class="nav-link" href="/logout">Log Out</a>
                        </li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container">
        <%# the length property below is because if there is not error, it won't be undefined. so without that second condition it won't work.%>
        <% if(error && error.length > 0) { %>
            <div class="alert alert-danger mt-3" role="alert">
                <%= error %>
            </div>
        <% } %>
        <% if(success && success.length > 0) { %>
        <div class="alert alert-success mt-3" role="alert">
            <%= success %>
        </div>
        <% } %>
    </div>
